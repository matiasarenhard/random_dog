<div class="flex min-h-screen items-center justify-center bg-gray-50 py-10 px-4 mx-auto">
  <div class="w-full max-w-sm bg-white border border-gray-200 rounded-lg shadow-sm dark:bg-gray-800 dark:border-gray-700 pt-20">
    
    <% if @dog_url.present? %>
      <img
        src="<%= @dog_url %>"
        alt="Random dog"
        class="p-8 rounded-t-lg w-full h-64 object-contain"
        loading="lazy"
      />
    <% else %>
      <div class="p-8 rounded-t-lg">
        <div class="w-full h-64 rounded-lg bg-gray-200 dark:bg-gray-700 animate-pulse"></div>
      </div>
    <% end %>

    <div class="px-5 pb-5">
      <h5 class="text-xl font-semibold tracking-tight text-gray-900 dark:text-white">
        Random Dog
      </h5>
      <p class="mt-2 mb-5 text-sm text-gray-700 dark:text-gray-400">
        Enjoy a random dog image fetched from Dog CEO API. Click the button below to see another dog!
      </p>

      <div class="flex items-center justify-between">
        <%= button_to "Next random dog",
              random_dog_path,
              method: :get,
              class: "text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800" %>
      </div>

      <% if @dog_url.blank? %>
        <div class="mt-4 text-sm font-semibold text-red-600 dark:text-red-400">
          Sorry, we couldn't fetch a dog image. Please try again!
        </div>
      <% end %>
    </div>
  </div>
</div>